函数返回值类型写在最后面
函数可以返回多个值，也可以给返回值命名，一般和error结合使用
函数式编程，函数也可以作为参数传递给其他函数
go语言中没有默认参数，可选参数，但是可以使用可变参数列表

函数的定义：
  func operate(a, b int, op string) int
  func swap(a, b int) (x, y int)
  func compute(op func(int, int) int, a, b int) int
  func sum(nums ...int) int

应⽤程序⼊⼝
    1. 必须是 main 包：package main
    2. 必须是 main ⽅法：func main()
    3. ⽂件名不⼀定是 main.go

    Go 中 main 函数不⽀持任何返回值
    通过 os.Exit(num) 来返回状态, num为0表示正常退出,其他表示异常退出

函数注意点
    1. 如果函数中有返回值,可以在返回值列表中定义变量,然后在函数体中直接使用
    2. 如果在返回值列表中定义了变量,那么return后面可以不用写任何内容,默认就会将返回值列表中定义的变量返回

函数可以返回多个值，也可以给返回值命名，返回多个值一般和error结合使用
go语言中没有默认参数，可选参数，但是可以使用可变参数列表

如果在同一个函数中编写了多个defer语句,那么会遵守先进后出的原则

init函数的作用
    对当前文件进行初始化
    会在main函数之前调用

main包中程序执行调用顺序
    main包 -> 常量 -> 全局变量 -> init函数 -> main函数 -> 执行逻辑代码 -> Exit程序
其他包中执行流程
    常量 -> 全局变量 -> init函数 -> 执行逻辑代码
当包中导入另外一个包,就会先进入另外一个包的执行流程,然后返回继续当前包的流程
不同包的 init 函数按照包导⼊的依赖关系决定执⾏顺序
每个包可以有多个 init 函数
包的每个源⽂件也可以有多个 init 函数，这点⽐较特殊